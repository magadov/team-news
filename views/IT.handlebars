<script>
  const allNews = (id) => {
    fetch("data.js")

    // const main = document.querySelector('.IT')
    .then(res => res.json())

    .then( (data) => {
      console.log(data)
      data.forEach(item => {
        const div = document.createElement('div')
        div.append(item.title)
        main.append(div)
      })
    })
  }
</script>